# Leave unset for local development, where Vite proxies /api to port 4000.
# Set this to the deployed API origin before building for production, with no
# trailing slash, for example https://knowledge-inbox-api.onrender.com
VITE_API_BASE_URL=
